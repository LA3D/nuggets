# Options in this directory apply to the historical slide archive.
# Keep the source directories in place so existing public URLs remain stable.

# freeze computational output
# (see https://quarto.org/docs/projects/code-execution.html#freeze)
freeze: true

# Enable banner style title blocks
title-block-banner: true
